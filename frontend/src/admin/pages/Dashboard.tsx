import { useEffect, useMemo, useState } from 'react';
import type { Cliente, CostoProduccion, EventoAdmin, Gasto, Pedido } from '../types';
import { fetchClientes } from '../../services/clientes';
import { fetchAsistentesEvento, fetchEventos } from '../../services/eventos';
import { fetchGastos } from '../../services/gastos';
import { fetchPedidos } from '../../services/pedidos';
import { fetchCostos } from '../../services/costos';

type AmountByDate = { fecha: string; monto: number };
type SeriesPoint = { label: string; ingresos: number; gastos: number };

const money = (value: number) => `$${Math.round(value).toLocaleString('es-CO')}`;
const percentage = (value: number, total: number) => total ? Math.round((value / total) * 100) : 0;
const monthKey = (date: string) => date.slice(0, 7);
const monthLabel = (key: string) => new Intl.DateTimeFormat('es-CO', { month: 'short' }).format(new Date(`${key}-02T12:00:00`)).replace('.', '');

function TrendChart({ points }: { points: SeriesPoint[] }) {
  const width = 620, height = 220, left = 34, right = 14, top = 16, bottom = 31;
  const maximum = Math.max(1, ...points.flatMap((point) => [point.ingresos, point.gastos]));
  const coordinates = (key: 'ingresos' | 'gastos') => points.map((point, index) => {
    const x = left + (index * (width - left - right)) / Math.max(1, points.length - 1);
    const y = top + (height - top - bottom) * (1 - point[key] / maximum);
    return `${x.toFixed(1)},${y.toFixed(1)}`;
  }).join(' ');
  return <svg viewBox={`0 0 ${width} ${height}`} className="h-56 w-full overflow-visible" role="img" aria-label="Gráfica de ingresos y gastos">
    {[0, .25, .5, .75, 1].map((step) => { const y = top + (height - top - bottom) * step; return <line key={step} x1={left} x2={width - right} y1={y} y2={y} stroke="#e9e5e0" strokeWidth="1" />; })}
    <polyline points={coordinates('ingresos')} fill="none" stroke="#5b9b48" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" />
    <polyline points={coordinates('gastos')} fill="none" stroke="#ff6d72" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" />
    {(['ingresos', 'gastos'] as const).flatMap((key) => points.map((point, index) => { const [x, y] = coordinates(key).split(' ')[index].split(','); return <circle key={`${key}-${point.label}`} cx={x} cy={y} r="3" fill="white" stroke={key === 'ingresos' ? '#5b9b48' : '#ff6d72'} strokeWidth="1.8" />; }))}
    {points.map((point, index) => <text key={point.label} x={left + (index * (width - left - right)) / Math.max(1, points.length - 1)} y={height - 7} textAnchor="middle" fontSize="10" fill="#918a93">{monthLabel(point.label)}</text>)}
  </svg>;
}

function MetricCard({ title, value, color, icon, details, badge }: { title: string; value: string; color: 'green' | 'red' | 'purple'; icon: string; details?: { label: string; value: string; percent: number }[]; badge?: string }) {
  const styles = { green: 'border-[#dbead9] text-[#4d983f] bg-[#f5fbf3]', red: 'border-[#ffd8d9] text-[#ff626a] bg-[#fff6f6]', purple: 'border-[#e8def9] text-[#8660c9] bg-[#faf7ff]' }[color];
  const fills = { green: 'bg-[#5b9b48]', red: 'bg-[#ff6d72]', purple: 'bg-[#9672d6]' }[color];
  return <section className={`relative overflow-hidden rounded-xl border bg-white p-5 shadow-[0_2px_10px_rgba(45,32,52,.04)] ${styles.split(' ')[0]}`}><div className={`absolute -right-8 -top-14 h-28 w-36 rounded-[45%] opacity-70 ${styles.split(' ')[3]}`} /><div className="relative"><div className="flex items-center gap-2"><span className={`grid h-7 w-7 place-items-center rounded-md border bg-white text-base ${styles.split(' ')[0]} ${styles.split(' ')[1]}`}>{icon}</span><span className={`text-[10px] font-medium uppercase tracking-wide ${styles.split(' ')[1]}`}>{title}</span></div><p className={`mt-3 font-serif text-[25px] font-semibold ${styles.split(' ')[1]}`}>{value}</p>{badge && <span className={`mt-2 inline-flex rounded-full px-2.5 py-1 text-[10px] ${styles.split(' ')[3]} ${styles.split(' ')[1]}`}>{badge}</span>}{details && <div className="mt-5 space-y-3">{details.map((detail) => <div key={detail.label}><div className="mb-1.5 flex items-center justify-between gap-2 text-[10px] text-ink/75"><span className="flex items-center gap-1.5"><i className={`h-1.5 w-1.5 rounded-full ${fills}`} />{detail.label}</span><span className="font-medium">{detail.value} ({detail.percent}%)</span></div><div className="h-1 rounded-full bg-ink/[.045]"><div className={`h-full rounded-full ${fills}`} style={{ width: `${detail.percent}%` }} /></div></div>)}</div>}</div></section>;
}

export default function Dashboard() {
  const [ventas, setVentas] = useState<Pedido[]>([]); const [gastos, setGastos] = useState<Gasto[]>([]); const [costos, setCostos] = useState<CostoProduccion[]>([]);
  const [clientes, setClientes] = useState<Cliente[]>([]); const [eventos, setEventos] = useState<EventoAdmin[]>([]); const [pagosTalleres, setPagosTalleres] = useState<AmountByDate[]>([]);
  const [error, setError] = useState(''); const [loading, setLoading] = useState(true);

  useEffect(() => { const load = async () => { try { setLoading(true); setError(''); const [loadedVentas, loadedGastos, loadedCostos, loadedClientes, loadedEventos] = await Promise.all([fetchPedidos(), fetchGastos(), fetchCostos(), fetchClientes(), fetchEventos()]); const pagos = await Promise.all(loadedEventos.map(async (evento) => ({ fecha: evento.fecha, monto: (await fetchAsistentesEvento(evento.id)).reduce((sum, asistente) => sum + asistente.pago, 0) }))); setVentas(loadedVentas); setGastos(loadedGastos); setCostos(loadedCostos); setClientes(loadedClientes); setEventos(loadedEventos); setPagosTalleres(pagos); } catch (err) { setError(err instanceof Error ? err.message : 'No se pudo cargar el resumen del negocio.'); } finally { setLoading(false); } }; void load(); }, []);

  const ingresosProductos = ventas.reduce((sum, venta) => sum + venta.total, 0); const ingresosTalleres = pagosTalleres.reduce((sum, pago) => sum + pago.monto, 0); const totalIngresos = ingresosProductos + ingresosTalleres;
  const gastosOperativos = gastos.reduce((sum, gasto) => sum + gasto.monto, 0); const costosProduccion = costos.reduce((sum, costo) => sum + costo.costoTotal, 0); const totalGastos = gastosOperativos + costosProduccion; const balance = totalIngresos - totalGastos;
  const series = useMemo(() => { const dated = [...ventas.map((item) => ({ fecha: item.fecha, ingresos: item.total, gastos: 0 })), ...pagosTalleres.map((item) => ({ fecha: item.fecha, ingresos: item.monto, gastos: 0 })), ...gastos.map((item) => ({ fecha: item.fecha, ingresos: 0, gastos: item.monto })), ...costos.map((item) => ({ fecha: item.fecha, ingresos: 0, gastos: item.costoTotal }))]; const keys = [...new Set(dated.map((item) => monthKey(item.fecha)).filter(Boolean))].sort().slice(-6); return keys.map((key) => dated.filter((item) => monthKey(item.fecha) === key).reduce((result, item) => ({ ...result, ingresos: result.ingresos + item.ingresos, gastos: result.gastos + item.gastos }), { label: key, ingresos: 0, gastos: 0 })); }, [ventas, pagosTalleres, gastos, costos]);
  const recientes = useMemo(() => [...ventas].sort((a, b) => b.fecha.localeCompare(a.fecha) || b.codigo.localeCompare(a.codigo)).slice(0, 5), [ventas]); const clientesPorId = useMemo(() => new Map(clientes.map((cliente) => [cliente.id, cliente.nombre])), [clientes]);
  const proximoTaller = useMemo(() => [...eventos].filter((evento) => evento.estado === 'Próximo').sort((a, b) => a.fecha.localeCompare(b.fecha) || a.hora.localeCompare(b.hora))[0], [eventos]);
  const costosPct = percentage(costosProduccion, totalGastos); const gastosPct = percentage(gastosOperativos, totalGastos);

  return <div className="max-w-[1380px]"><h1 className="mb-1 font-serif text-2xl font-semibold text-ink">Dashboard</h1><p className="mb-7 text-sm text-ink/55">Resumen general del negocio</p>{loading && <p className="mb-6 rounded-xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink/55">Cargando información...</p>}{error && <p className="mb-6 rounded-xl border border-danger/20 bg-danger/5 px-4 py-3 text-sm text-danger">{error}</p>}{!loading && !error && <><div className="grid gap-4 xl:grid-cols-3"><MetricCard title="Ingresos totales" value={money(totalIngresos)} color="green" icon="▣" badge="Ingresos de productos y talleres" details={[{ label: 'Productos', value: money(ingresosProductos), percent: percentage(ingresosProductos, totalIngresos) }, { label: 'Talleres', value: money(ingresosTalleres), percent: percentage(ingresosTalleres, totalIngresos) }]} /><MetricCard title="Gastos totales" value={money(totalGastos)} color="red" icon="▤" badge="Costos de producción y gastos" details={[{ label: 'Costos de producción', value: money(costosProduccion), percent: costosPct }, { label: 'Gastos operativos', value: money(gastosOperativos), percent: gastosPct }]} /><MetricCard title="Balance del período" value={money(balance)} color="purple" icon="⌁" badge={balance >= 0 ? '● Resultado positivo' : '● Resultado negativo'} details={[{ label: 'Ingresos totales', value: money(totalIngresos), percent: 100 }, { label: 'Gastos totales', value: money(totalGastos), percent: totalIngresos ? Math.min(100, percentage(totalGastos, totalIngresos)) : 0 }]} /></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.15fr_.85fr]"><section className="rounded-xl border border-ink/10 bg-white p-5 shadow-[0_2px_10px_rgba(45,32,52,.04)]"><div className="flex items-start justify-between"><div><h2 className="font-serif text-base font-semibold">Ingresos vs Gastos</h2><div className="mt-2 flex gap-5 text-[10px] text-ink/60"><span className="flex items-center gap-1.5"><i className="h-0.5 w-3 bg-[#5b9b48]" />Ingresos</span><span className="flex items-center gap-1.5"><i className="h-0.5 w-3 bg-[#ff6d72]" />Gastos</span></div></div><span className="rounded-md border border-ink/10 px-2.5 py-1.5 text-[10px] text-ink/65">Últimos meses</span></div>{series.length ? <TrendChart points={series} /> : <p className="grid h-56 place-items-center text-sm text-ink/45">Aún no hay movimientos para graficar.</p>}</section><section className="rounded-xl border border-ink/10 bg-white p-5 shadow-[0_2px_10px_rgba(45,32,52,.04)]"><h2 className="font-serif text-base font-semibold">Distribución de gastos</h2><div className="mt-4 flex flex-col items-center gap-6 sm:flex-row"><div className="grid h-32 w-32 shrink-0 place-items-center rounded-full" style={{ background: `conic-gradient(#9672d6 0 ${gastosPct}%, #ffd15b ${gastosPct}% 100%)` }}><div className="grid h-[78px] w-[78px] place-items-center rounded-full bg-white text-center"><span className="text-[10px] text-ink/55">Total</span><strong className="-mt-3 text-xs">{money(totalGastos)}</strong></div></div><div className="w-full space-y-4 text-xs"><div className="flex items-start justify-between gap-3"><span className="flex gap-2 text-ink/70"><i className="mt-1 h-2 w-2 rounded-full bg-[#9672d6]" />Gastos operativos</span><span className="text-right font-medium">{money(gastosOperativos)}<small className="ml-2 text-ink/55">{gastosPct}%</small></span></div><div className="flex items-start justify-between gap-3"><span className="flex gap-2 text-ink/70"><i className="mt-1 h-2 w-2 rounded-full bg-[#ffd15b]" />Costos de producción</span><span className="text-right font-medium">{money(costosProduccion)}<small className="ml-2 text-ink/55">{costosPct}%</small></span></div></div></div><p className="mt-5 rounded-md bg-[#f3edfa] px-3 py-2 text-[10px] text-[#72569e]">ⓘ La distribución muestra cómo se componen los gastos totales.</p></section></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-xl border border-ink/10 bg-white p-5"><h2 className="mb-3 font-serif text-base font-semibold">Ventas recientes</h2>{recientes.length ? recientes.map((venta) => <div key={venta.id} className="flex items-center justify-between border-t border-ink/7 py-2.5 text-sm"><span><strong>{clientesPorId.get(venta.clienteId) ?? 'Cliente eliminado'}</strong><small className="ml-2 text-ink/45">{venta.fecha}</small></span><strong>{money(venta.total)}</strong></div>) : <p className="py-4 text-sm text-ink/45">Aún no hay ventas registradas.</p>}</section><section className="rounded-xl border border-ink/10 bg-white p-5"><h2 className="mb-3 font-serif text-base font-semibold">Próximo taller</h2>{proximoTaller ? <><strong>{proximoTaller.nombre}</strong><p className="mt-1 text-sm text-ink/60">{proximoTaller.fecha} · {proximoTaller.hora}</p><p className="mt-1 text-sm text-ink/60">{proximoTaller.ubicacion}</p></> : <p className="py-4 text-sm text-ink/45">No hay talleres próximos programados.</p>}</section></div></>}</div>;
}
