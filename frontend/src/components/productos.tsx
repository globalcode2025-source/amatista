import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import type { ProductoAdmin } from '../admin/types'; import { fetchProductos, resolveProductoImage } from '../services/productos';
export function Productos(){const [items,setItems]=useState<ProductoAdmin[]>([]);useEffect(()=>{fetchProductos().then(setItems).catch(()=>setItems([]))},[]);return <section id="productos" className="py-[120px]"><div className="mx-auto max-w-[1180px] px-8"><div className="mb-16 max-w-[640px]"><span className="font-hand text-[1.3rem] text-gold">De nuestras manos a tu espacio</span><h2 className="mt-2 font-serif text-[clamp(2.1rem,3.6vw,3rem)] font-semibold text-ink">Productos</h2></div><div className="grid grid-cols-1 gap-7 sm:grid-cols-2 lg:grid-cols-3">{items.map(p=><article key={p.id} className="group flex flex-col overflow-hidden rounded-sm border border-ink/8 bg-white"><img src={resolveProductoImage(p.imagen)} alt={p.nombre} className="aspect-square w-full object-cover"/><div className="flex flex-1 flex-col p-6 pb-[26px]"><span className="font-hand text-[1.05rem] text-gold">{p.categoria}</span><h3 className="mb-2 mt-1.5 font-serif text-[1.28rem] font-semibold text-ink">{p.nombre}</h3><p className="flex-1 text-sm leading-relaxed text-ink/62">{p.descripcion}</p><div className="mt-4 flex items-center justify-between border-t border-ink/8 pt-4"><span className="font-serif text-[1.15rem] font-semibold text-ink">${p.precio.toLocaleString('es-CO')}</span><a href="#" className="text-[.78rem] uppercase tracking-wide text-amatista-mid">Comprar →</a></div></div></article>)}</div><div className="mt-14 text-center"><Link to="/catalogo" className="inline-block rounded-sm border border-amatista-deep px-8 py-4 text-sm uppercase tracking-wider text-amatista-deep">Ver colección completa</Link></div></div></section>}; export default Productos;
