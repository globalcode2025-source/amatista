# Amatista

## Iniciar el backend

Desde esta carpeta ejecuta:

```powershell
.\run-backend.cmd
```

También puedes usar el comando equivalente:

```powershell
.\.venv\Scripts\python.exe -m uvicorn --app-dir .\backend app.main:app --reload
```
